# RustKissVDB

DB KISS en Rust: **State Store + Event Store + SSE + Vector Store**.

## Quickstart

- Run: `cargo run`
- SSE: `curl -N "http://localhost:8080/v1/events" -H "Authorization: Bearer dev"`

Documentación: `docs/` (arquitectura, API, config, demo, bench, OpenAPI).
